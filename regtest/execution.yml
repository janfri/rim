---
sample: rake -n aspell
result:
- "** Execute (dry run) aspell"
---
sample: rake -n aspell:check
result:
- "** Execute (dry run) aspell:check"
---
sample: rake -n clean
result:
- "** Execute (dry run) clean"
---
sample: rake -n clobber
result:
- "** Execute (dry run) clean"
- "** Execute (dry run) clobber_rdoc"
- "** Execute (dry run) clobber"
---
sample: rake -n clobber_rdoc
result:
- "** Execute (dry run) clobber_rdoc"
---
sample: rake -n gem
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) gem:build"
- "** Execute (dry run) test"
- "** Execute (dry run) version:check"
- "** Execute (dry run) gem"
---
sample: rake -n gem:push
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) gem:build"
- "** Execute (dry run) test"
- "** Execute (dry run) version:check"
- "** Execute (dry run) gem"
- "** Execute (dry run) gem:push"
---
sample: rake -n git:check
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
---
sample: rake -n git:push
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) git:push"
---
sample: rake -n git:tag
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) gem:build"
- "** Execute (dry run) test"
- "** Execute (dry run) version:check"
- "** Execute (dry run) gem"
- "** Execute (dry run) gem:push"
- "** Execute (dry run) git:tag"
---
sample: rake -n info
result:
- "** Execute (dry run) info"
---
sample: rake -n install
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) gem:build"
- "** Execute (dry run) test"
- "** Execute (dry run) version:check"
- "** Execute (dry run) gem"
- "** Execute (dry run) install"
---
sample: rake -n rdoc
result:
- "** Execute (dry run) doc/index.html"
- "** Execute (dry run) rdoc"
---
sample: rake -n release
result:
- "** Execute (dry run) aspell:check"
- "** Execute (dry run) git:check"
- "** Execute (dry run) gem:build"
- "** Execute (dry run) test"
- "** Execute (dry run) version:check"
- "** Execute (dry run) gem"
- "** Execute (dry run) gem:push"
- "** Execute (dry run) git:tag"
- "** Execute (dry run) release"
---
sample: rake -n rerdoc
result:
- "** Execute (dry run) clobber_rdoc"
- "** Execute (dry run) doc/index.html"
- "** Execute (dry run) rdoc"
- "** Execute (dry run) rerdoc"
---
sample: rake -n test
result:
- "** Execute (dry run) test"
---
sample: rake -n uninstall
result:
- "** Execute (dry run) uninstall"
---
sample: rake -n version:check
result:
- "** Execute (dry run) version:check"
